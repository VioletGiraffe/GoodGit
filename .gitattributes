# CI runs these on macOS and Linux, where CRLF breaks the shebang line
*.sh text eol=lf

# cmd.exe mis-parses LF-only batch files
*.bat text eol=crlf

# The diff parser matches git's LF-terminated headers exactly; CRLF breaks it
tests/fixtures/*.diff text eol=lf
